# Replication

## Goal
Keep the secondary host aligned without replacing the primary node identity.

## Scope
- config copies
- state comparison
- audit snapshots
- port/user alignment checks

## Tooling location
- `scripts/sync/`
- `scripts/verify/`
