# M4 Secondary Build

## Purpose
Maintain a secondary ClearingHouse-capable host for validation, sync, and continuity.

## Responsibilities
- receive mirrored config
- compare service state
- support reverse-repo logic
- host future orchestration and audit helpers

## Rule
Do not expose the M4 host as the canonical federated node by default.
